# Copie para .env e preencha. O .env real NUNCA é commitado.

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# String de conexão Postgres (Supabase → Connect → ORMs → Drizzle, transaction pooler)
DATABASE_URL=
